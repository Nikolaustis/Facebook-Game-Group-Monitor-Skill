# V6.6.4 Overlay Instructions

1. Stop any active Facebook Group Monitor process before replacing files.
2. Extract this archive into the existing Skill root and replace matching files.
3. Preserve the existing directories:

```text
runs/
config/
node_modules/
```

4. No new npm dependency is required.
5. Run a fresh second round for corrected final output. A non-finalized checkpoint can also resume; strong staged title matches will be revalidated automatically.
6. Existing finalized XLSX files are not rewritten automatically.
