# V5.2.1 Validation

验证目标：人工复核候选只有在成员规模和活跃度均达到门槛后才写入 `manual_review`。

检查项：

- JavaScript 语法检查通过。
- JSON 配置解析通过。
- `manual_review` 输出列包含 `group_size`、`today_posts`、`week_new_fans`。
- 主脚本与中断恢复脚本使用相同人工复核列。
- 阈值评估支持：规模不达标、活跃不达标、今日帖子达标、周新增达标四种分支。
