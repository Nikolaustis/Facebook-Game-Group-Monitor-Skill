# V6.6.1 Validation

## Static validation

- JavaScript syntax checks pass for all active scripts.
- Package and lockfile versions are `6.6.1`.
- No active file reads `process.env.CODEX_CLI_PATH`.
- No documentation recommends creating `CODEX_CLI_PATH`.
- Error messages reference PATH, local `codex_exec.command`, or `FB_MONITOR_CODEX_CLI_PATH`.
- The semantic resolver removes the legacy variable from discovery and Codex child-process environments.
- The uploaded workbook output-column order remains unchanged from the uploaded base package.

## Targeted behavior validation

- A fake legacy `CODEX_CLI_PATH` candidate is ignored.
- A fake `FB_MONITOR_CODEX_CLI_PATH` candidate is discovered.
- A mock standalone Codex CLI receives no `CODEX_CLI_PATH` in its child environment and passes version/login preflight.
- Diagnostics report legacy-variable presence as booleans without exposing the value.
- The explicit cleanup script does not run during installation or collection.

## Environment limitation

The container cannot launch the Windows desktop application or Windows Task Scheduler. Final confirmation consists of restarting the desktop app and running `npm run semantic:verify-codex` on the user’s Windows machine.
