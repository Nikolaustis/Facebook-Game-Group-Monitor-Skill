# V5.3.0 Validation

## JavaScript 语法

- `node --check scripts/phase2_collect_details.js`：通过。
- `node --check scripts/finalize_partial_xlsx.js`：通过。

## 定向逻辑测试

- `Cookie Run Kingdom ESPAÑOL❤️` 删除标题后只保留 `ESPAÑOL❤️`。
- 西班牙语帖子包含重复游戏标题时，最终语言为 `Spanish`。
- `Cookie Run: Kingdom Buy and Sell International` 不产生 GeoNames 候选。
- `Cookie Run Kingdom ESPAÑOL❤️` 不产生 GeoNames 候选。
- `Cookie Run Kingdom Paris` 只产生 `paris`，不含 `cookie / run / kingdom`。

## XLSX 格式测试

- K2/L2 均为公式单元格。
- K2/L2 使用内置 numFmtId 10，即 `0.00%`。
- 写入时启用 `cellStyles: true`。
