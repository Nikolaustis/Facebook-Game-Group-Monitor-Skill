# V6.6.0 Validation

Validation scope:

- JavaScript syntax for all packaged `.js` files.
- JSON syntax for package metadata and assets.
- UTF-8, UTF-8 BOM, UTF-16LE BOM, and UTF-16BE BOM JSON decoding.
- Phase-2 input validation across index, config, shutdown policy, and candidate files.
- Supervisor success only after a fresh progress checkpoint.
- Supervisor detection of immediate child failure.
- API-first semantic resolver order retained.
- Uploaded workbook field-order code preserved except for input-decoding and candidate-path resolution changes.
- Package excludes `runs`, `config`, `node_modules`, temporary diagnostics, and older version validation/incident documents.

Windows Task Scheduler, Facebook CDP collection, real shutdown, and a full overnight handoff require final verification on the user’s Windows machine.

Recorded validation results in the build environment:

```text
ALL_JSON_OK
POWERSHELL_STATIC_OK
UTF8_BOM_OK
UTF16LE_BOM_OK
UTF16BE_BOM_OK
PHASE2_INPUT_PREFLIGHT_OK
SUPERVISOR_PROGRESS_HEALTH_OK
SUPERVISOR_IMMEDIATE_FAILURE_OK
XLSX_FIELD_ORDER_SHA256=920db6f478680bd044f07ea44eed9d217790d64b5221d318fb9194697430d9e6
```
