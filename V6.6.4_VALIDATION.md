# V6.6.4 Validation

## Static validation

- `phase2_collect_details.js`: JavaScript syntax passed.
- All package JSON files parsed successfully.
- The authoritative XLSX field array was not reordered.
- No `/runs`, `/config`, or `/node_modules` content is included.

## Same-business-region regression

Input:

```text
Grow a garden2 Laos🇱🇦 Thai🇹🇭 ຊື້ຂາຍແລກປ່ຽນ ชื้อขายแลกเปลี่ยน
```

Observed:

```text
region=SEA
source=country_keyword_and_flag_same_business_region
raw evidence=TH, LA, LA flag, TH flag
hasMultipleRegionEvidence=false
```

## Short-alias regression

Observed results:

```text
GAG Thailand Trading       → Grow a Garden accepted
Gagged Lovers              → no match
Just For Laughs Gags       → no match
9gag mexico                → no match
GAG2 trading               → Grow a Garden rejected; Grow a Garden 2 accepted
GAG 2 Indonesia            → Grow a Garden rejected; Grow a Garden 2 accepted
Grow a Garden 2 Thailand   → Grow a Garden rejected as more-specific sibling
```

A name containing two independent explicit titles, such as `Grow a Garden / GAG 2`, remains eligible for both games under the existing multi-game rule.

## Uploaded-run regression

The uploaded prior-version checkpoint contained 570 staged rows. Conservative resume revalidation under V6.6.4 produced:

```text
rows before=570
rows retained=545
rows removed=25
```

The removed set consisted of the reported false-positive classes: ordinary `gags`/`gagged`/`9gag` names and `GAG2`/`GAG 2` rows previously attributed to `Grow a Garden`.

## Environment limitation

The container cannot execute Facebook CDP collection, Windows Task Scheduler, or an actual shutdown. Runtime browser and scheduler behavior remains subject to confirmation on the user's Windows machine. The deterministic title, region, resume-revalidation, JavaScript syntax, and package-integrity tests were executed locally.
