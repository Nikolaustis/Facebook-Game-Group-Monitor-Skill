# V6.5.4 Validation

## Static validation

- `scripts/semantic_region_resolver.js`: Node syntax check passed.
- `scripts/diagnose_semantic_provider_chain.js`: Node syntax check passed.
- `scripts/diagnose_codex_exec.js`: Node syntax check passed.
- Package JSON, task template, output Schema, and public local-config example parsed successfully.
- Package contains only V6.5.4 validation and incident-analysis documents.
- No private `semantic_model.local.json` or API key is included.

## API-only diagnostic matrix

A local OpenAI-compatible mock endpoint was used with Codex disabled.

| API outcome | Final provider | Diagnostic status | Exit code |
|---|---|---|---:|
| Valid decision, confidence 0.96 | custom API | `api_decision_accepted` | 0 |
| Valid decision, confidence 0.40 | rules only after fallback | `api_valid_low_confidence_fallback` | 0 |
| Invalid JSON | rules only after failure | `api_request_or_parse_failed` | 2 |

The low-confidence case recorded:

```text
api_requests = 1
api_errors = 0
api_returned_valid_decision = true
low_confidence_fallback_observed = true
```

## Runtime boundary

The validation did not call the user's paid API, Codex account, Facebook, Windows Task Scheduler, or shutdown command. Run `npm run semantic:verify-api` once on the target Windows machine after applying the overlay.
