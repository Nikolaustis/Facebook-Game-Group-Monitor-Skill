# V5.7.0 Validation

## Group-name prefilter cases

- `ArcheAge War Global` -> `strong_match`, continue to About.
- `ArcheAge Community` with IP root `ArcheAge` -> `manual_review_match`, continue to About.
- `Last War: Survival Game` -> `skip_no_match`, do not open About/discussion.
- missing group name / seed URL -> `inconclusive`, continue to About.
- truncated `ArcheAge War…` -> `inconclusive`, continue to About.

## Static checks

- `node --check scripts/phase2_collect_details.js`
- `package.json`, `package-lock.json`, and `assets/task_config.template.json` parse successfully.
- V5.6 GeoNames, XLSX, manual-review, autosave, and forced-shutdown logic remains present.
