# V5.5.0 验证记录

## 静态校验

- `node --check scripts/phase2_collect_details.js`：通过。
- `node --check scripts/finalize_partial_xlsx.js`：通过。
- package、task config、GeoNames example JSON：通过解析。

## 定向样例

以下群名生成的群名 GeoNames queries 均为空：

- `Cookierun: Ovenbreak & Classic 🍪`
- `Cookierun Classic ซื้อขายรหัส`
- `LINE Idle Rangers ศูนย์รวมซื้อขายไอดี Talk&Trade`
- `🟢 LINERANGERS - ซื้อขายพูดคุย GREEN-TOWN 🏡`
- `🛠️LINE RANGERS ซื้อขายแลกเปลี่ยน@วิน🎈`
- `Pecinta Akun KUNING Line Rangers (Jual/Beli)`

`CookieRun Kingdom Paris` 仍保留 `paris` query。

## 语言与地区优先级

- 三条泰语 LINE Rangers 样例在 About 为空时均输出 `TH`，来源为 `language_map_before_group_name_geocoder`。
- 印尼语 `Jual/Beli` 样例输出 `ID`。
- About 为 `Simla` 时，本地模拟 GeoNames 返回印度 Shimla，最终输出 `South Asia`，且只请求 About query；About 为空时 Thai 输出 `TH` 且不发起群名请求。

## GeoNames 精确匹配

- 本地模拟 `silvermoon -> Silvermoon Village`：拒绝为 `rejected_context_mismatch`。
- 本地模拟 `boston -> Boston`：accepted，输出 `North America`。
- 孤立泰语 `วิน`：`unsafe_query`。

## 回归

- detail/manual_review 列结构未改变。
- K/L 百分比、恢复导出、后台任务和关机脚本未修改。
