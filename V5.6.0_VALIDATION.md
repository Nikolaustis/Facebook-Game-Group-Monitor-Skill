# V5.6.0 验证记录

## 静态检查

- `node --check scripts/phase2_collect_details.js`：通过。
- `node --check scripts/finalize_partial_xlsx.js`：通过。
- package/task config JSON：通过。

## 地名候选测试

- `Pokemon Go Paris` -> `paris`。
- `Pokemon Go San Diego` -> `san diego`，不生成 `diego`。
- `Pokemon Go El Paso TX` -> `el paso tx | el paso`，不生成 `paso`。
- `Talk&Trade / GREEN-TOWN / Jual/Beli / Mua Bán / Come Together / League Community` -> 无 query。
- `PokeMonedas / Pok'emon` -> 无残词 query。
- `Orange County / Victoria BC` -> 保留完整短语。

## 本地地区测试

- `ID Thailand` -> `TH`。
- `™` -> 不产生 `TM`。
- `Trójmiasto` -> `PL`。
- `Danmark` -> `EUR`。
- `Hàn Quốc` -> `KR`。
- `LATHAM / GDL` -> `LATAM`。
- `SEQ Brisbane` -> `Oceania`。
- `Arabs` -> `Middle East`。
- 孤立 `Georgia` -> 不本地强判。

## GeoNames accepted 结果测试

- 单 token 小型普通 PPL（人口 1,200）-> `rejected_context_mismatch`。
- `Boston`（PPLA2，人口 650,000）-> `accepted`。
- `Oregon`（ADM1）-> `accepted`。
